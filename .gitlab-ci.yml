stages:
  - build
  - test
  - deploy

build-job:
  tags:
    - atria
  stage: build
  script:
    - cd cowem-plugin
    - ./gradlew build deployReports -Dorg.gradle.project.buildNumber=$CI_BUILD_REF
  only:
    - master

