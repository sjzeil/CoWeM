/*
 * CoWem course website v1 build file
 * Steven J Zeil, Old Dominion Univ.
 */
/*
 * This is the "normal dependency set. But to avoid the need to republish every
 * every tewak made to the cowem project, I'm not using it for this reference
 * manual.
*/ 
 buildscript { 
     repositories {
         jcenter()
         mavenCentral()
 
         // Use my own CS dept repo
         ivy {
             url 'https://secweb.cs.odu.edu/~zeil/ivyrepo'
         }
     }
     dependencies {
         classpath 'org.hidetake:gradle-ssh-plugin:2.3.0'
         classpath 'edu.odu.cs.zeil:cowem-plugin:1.0+'
     }
}
/* */

 /* 
 buildscript {  // special dependencies uses local cowem project
     repositories {
         jcenter()
         mavenCentral()
     }
     dependencies {
         classpath 'org.hidetake:gradle-ssh-plugin:2.3.0'
         classpath fileTree (dir:'../cowem/core/build/libs/', include: '*.jar')
         classpath fileTree (dir:'../cowem/utils/build/libs/', include: '*.jar')
         
         // But then I have to explicitly copy their dependencies
         classpath 'org.hidetake:gradle-ssh-plugin:2.2.0+'
         classpath 'org.pegdown:pegdown:1.6.0+'
         classpath 'org.slf4j:slf4j-api:1.7.0+'
         classpath 'net.sf.saxon:saxon-dom:8.7+'
     
     }
}
*/
  
apply plugin: edu.odu.cs.cowem.CourseWebsite


 

course {
	courseName        = 'CoWeM v1.0'
	courseTitle       = 'Course Websites frm Markdown'
	semester          = '2016'
	sem               = 'latest'
	instructor        = 'Steven J Zeil'
	email             = 'zeil@cs.odu.edu'
	copyright         = '2016, Old Dominion Univ.'
	homeURL           = 'http://www.cs.odu.edu/~zeil/temp/cowem/Directory/outline/'
	deployDestination = '/home/zeil/public_html/temp/cowem/'
	sshDeployURL      = 'zeil@atria.cs.odu.edu:/home/zeil/public_html/cowem/'
    baseURL           = 'http://www.cs.odu.edu/~zeil/temp/cowem/'
}

 

 
